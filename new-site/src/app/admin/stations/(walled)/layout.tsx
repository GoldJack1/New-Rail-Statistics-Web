/**
 * Stations list at `/admin/stations` mirrors the public browse page at `/stations`.
 * Admin write routes remain under `admin/(authenticated)/stations/`.
 */
export default function WalledStationsBrowseLayout({ children }: { children: React.ReactNode }) {
  return children
}
